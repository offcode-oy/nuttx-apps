#
# For a description of the syntax of this configuration file,
# see the file kconfig-language.txt in the NuttX tools repository.
#

config EXAMPLES_NNG
	tristate "libnng examples"
	default n
	depends on NETUTILS_LIBNNG
	---help---
		Enable the NNG examples

if EXAMPLES_NNG


endif
